name: Deploy slides

# Builds app/ and publishes it to GitHub Pages, with the legacy reveal.js deck
# kept alongside at /deck/ as a fallback.
#
# One-time repository setting: Settings → Pages → Build and deployment →
# Source = "GitHub Actions". Until that is switched over, Pages keeps serving
# the old branch contents and this workflow's output is ignored.

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# let a running deploy finish rather than cancelling it mid-publish
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Install
        working-directory: app
        run: npm ci

      - name: Build
        working-directory: app
        run: npm run build

      - name: Keep the old reveal.js deck available at /deck/
        run: cp -r deck app/dist/deck

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: app/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
