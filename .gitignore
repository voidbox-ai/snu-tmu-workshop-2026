# working state the server writes — never worth versioning
.backups/
assets/_replaced/

# python / node scratch
__pycache__/
*.pyc
node_modules/
package-lock.json
dist/

# ...but the app's lockfile IS versioned: the deploy workflow runs `npm ci`,
# which refuses to install without it.
!app/package-lock.json

# leftovers from a failed setup attempt
_to_delete/

# os junk
.DS_Store
Thumbs.db
desktop.ini
